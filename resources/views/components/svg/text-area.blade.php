<textarea
    id="{{ $id }}"
    name="{{ $name }}"
    rows="{{ $rows }}"
    class="block {{ $class }} block p-4 px-10 py-5 md:py-7 w-full h-96 md:h-[400px] text-sm border-gray-300 focus:border-primary focus:primary rounded-md shadow-sm"
    {{ $attributes }}
>
    {{ $value }}
</textarea>

